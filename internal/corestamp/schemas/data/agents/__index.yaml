_meta:
  version: "1.0.0"
  schema_id: "lightwave://schemas/data/agents/__index"
  title: "Agent Schemas Registry"
  description: |
    Forgejo-shaped CI action-runner registration only. Live agent
    runtime_status is agent_identity + a nullclaw instance under nullhub
    (SAD-0002).
    Do not read `agent_runner` as a nulltickets worker or a nullclaw process.

schemas:
  agent_runner: agent_runner.yaml
