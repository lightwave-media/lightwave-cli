_meta:
  version: "0.1.0"
  schema_id: "lightwave://schemas/workflows/actions/__index"
  title: "Actions Registry"
  description: "Atomic parameterized primitives. First entry is the ontology grid census."

schemas:
  ontology_census: ontology_census.yaml
